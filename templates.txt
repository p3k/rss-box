<script type='text/plain' class='stylesheet template'>
   #${id} {
      width: ${width}px;
      border: 1px solid ${frameColor};
      font: ${fontFace};
      float: ${align};
      overflow: hidden;
      border-radius: ${radius}px;
      -moz-border-radius: ${radius}px;
   }
   #${id} a {
      color: ${linkColor};
      text-decoration: none;
   }
   #${id} a:hover {
      text-decoration: underline;
   }
   #${id} .rssbox-titlebar {
      padding: 5px 3px 5px 10px;
      color: ${titleBarTextColor};
      background-color: ${titleBarColor};
   }
   #${id} .rssbox-titlebar a {
      color: ${titleBarTextColor};
   }
   #${id} .rssbox-content {
      padding: 5px 10px;
      background-color: ${boxFillColor};
      border-top: 1px solid ${frameColor};
   }
   #${id} .rssbox-item-content {
      margin-bottom: 0.8em; 
      color: ${textColor};
   }
   #${id} form {
      margin-bottom: 0.8em;
   }
   .rssbox-date {
      margin-top: 1px;
      font-size: 80%;
   }
   .rssbox-promo {
      margin: 0 -5px -1px 0; 
      text-align: right; 
      font-size: 7pt; 
      letter-spacing: 1px;
   }
</script>

<script type='text/plain' class='box template'>
   <div id='${id}' class='rssbox ${id} rssBox'>
      <div class='rssbox-titlebar'>
         <div style='float: right;'>${xmlButton}</div>
         <div style='font-weight: bold;'>${title}</div>
         <div class='rssbox-date'>${date}</div>
      </div>
      <div class='rssbox-content rssBoxContent'>
         ${image}
         ${items}
         ${input}
         <div class='rssbox-promo rssBoxPromo'>
            <a href='http://p3k.org/rss'>RSS Box by p3k.org</a>
         </div>
      </div>
   </div>
</script>

<script type='text/plain' class='item template'>
<div class='rssbox-item-content rssBoxItemContent'>
   <div style='font-weight: ${fontWeight};'>${title}</div>
   ${description}
   ${buttons}
</div>
</script>

<script type='text/plain' class='image template'>
   <a href="${link}"><span title='${title}' class='${class}' style='display: ${display}; float: ${align}; height: ${height}px; width: ${width}px; margin: ${vspace}px ${hspace}px; padding-left: ${padding}px; background-image: url(${source}); background-position: left center; background-repeat: no-repeat;'></span></a>  
</script>

<script type='text/plain' class='input template'>
   <form method="get" action="${link}">
   <input type="text" name="${name}" size="15" placeholder='${description}'> 
   <input type="submit" value="${title}">
   </form>
</script>

<script type='text/plain' class='link template'>
   <a class="${class}" href="${link}">${text}</a>
</script>

<script type='text/plain' class='date template'>
   ${year}-${month}-${day}, ${hours}:${minutes}h
</script>

<script type='text/plain' class='error template'>
   <?xml version="1.0"?>
   <rss version="error">
      <channel>
         <title>RSS Box Error</title>
         <link>${link}</link>
         <description>This output was automatically generated to report an error 
         that occurred during a request to the JavaScript RSS Box Viewer.</description>
         <item>
            <title>Oops, something went wrong...</title>
            <description>An error occurred while processing the request to the 
            JavaScript RSS Box Viewer.</description>
         </item>
         <item>
            <title>The following error message was returned:</title>
            <description>${message}</description>
         </item>
         <item>
            <description>Most likely, this might have happened because of a 
            non-existent or invalid RSS feed URL. Please check and possibly correct 
            your input, then try again.</description>
         </item>
      </channel>
   </rss>
</script>

